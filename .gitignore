# 不提交 Selene token（含敏感凭证）
selene_token.txt

# 云函数配置含 SELENE_TOKEN 环境变量，禁止提交（本地保留即可）
cloudbaserc.json

# 部署产物（由脚本生成并推送到 CloudBase）
deploy/

# 实时拉取的快照为临时数据，重新 fetch 即可生成
gantt_live.json

# 差异轮询用的本地状态指纹（重新 fetch 会自动重建）
last_fetch_state.txt

# Python 缓存
__pycache__/
*.pyc
